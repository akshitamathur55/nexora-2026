// Architecture.md section 7: all reads/writes go through the Express backend
// (Admin SDK), never directly from the browser. This is the enforced deny-all.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
